import { IntersectImgDirective } from './intersect-img.directive';

describe('IntersectImgDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectImgDirective();
    expect(directive).toBeTruthy();
  });
});
